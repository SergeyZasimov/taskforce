export enum RouteDomain {
  Users = 'http://localhost:3332/api',
  Tasks = 'tasks',
}

export enum RouteModule {
  Auth = 'auth',
}

export enum Route {
  Register = 'register',
  Login = 'login',
  ChangePassword = 'change-password',
}
