export enum ChangeTaskCounter {
  AllTasks = 'tasksCount',
  NewTasks = 'newTasksCount',
  CompletedTasks = 'completedTasksCount',
  FailedTasks = 'failedTasksCount',
}

export enum ChangeTasksCounterDirection {
  Inc = '1',
  Decr = '-1',
}
